"use strict";let files=App.uploadFiles(),form=document.querySelector("form#addProducts");form.addEventListener("submit",event=>{event.preventDefault();let id=form.getAttribute("id"),method=form.getAttribute("method"),action=form.getAttribute("action"),payload={};payload.images=files;let formData=App.processForms(id,payload);console.log(formData),App.form(formData,method,action,"/products/all")});